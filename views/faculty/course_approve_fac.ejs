<%- include('../partials/header1') %>

<div class="box" id="heading">
    <h1 class="main-heading">
      <%=listTitle%> 
    </h1>

        <div class="item">

              <table class="table table-hover table-dark">
                <thead>
                  <tr>
                    
                    <th scope="col">Name</th>
                    <th scope="col">Branch</th>
                    <th scope="col">Program</th>
                    <!-- <th scope="col">Approve</th> -->

                    <!-- <th scope="col"></th> -->

                  </tr>
                </thead>

                <% for(let item of listItems){%>
                <tbody>

                  <tr>
                    
                    <td id="title<%=item.id%>">
                      <%= item.name %>
                    </td>
                    <td id="title<%=item.id%>">
                      <%= item.branch %>
                    </td>
                    <td id="title<%=item.id%>">
                      <%= item.program %>
                   </td>
                   <!-- <td>
                    <select name="branch" class="form-control">
                      <option value="true">true</option>
                      <option value="false">false</option>
                    </select>
                   </td> -->
                    
                  </tr>
                  
                </tbody>
                <% } %>
              </table>

              
<!-- 
              <form action="/courseapprovaldone" method="POST">
              
                <input type="text" class="form-control" name="code" value="true" hidden>

              <button type="submit" class="btn btn-primary btn-lg px-4 gap-3">Course Approval</button>
            </form> -->
            <div class="col-lg-6 mx-auto">

            <form action="/deletestudent" method="POST">

              <p class="lead mb-4 main-heading">DELETE STUDENT:</p>
              <div class="form-group reg">
                <select name="name" class="form-control">
            <% for(let item of listItems){%>
                  <option value="<%=item.name%>"><%=item.name%></option>
            <% } %>
                </select>
              </div>

              <div class="form-group reg">
                <input type="text" class="form-control" name="code" value="<%=listTitle%>" hidden>
              </div>
             
              <div class="reg">
              <button type="submit" class="btn btn-primary btn-lg px-4 gap-3" <%=hidden%>>Remove</button>
            </div>

            </form>

</div>

              <form action="/courseapproval" method="POST">
                <input type="text" class="form-control" name="code" value="<%=listTitle%>" hidden>
                <button type="submit" class="btn btn-primary btn-lg px-4 gap-3" <%=hidden%>>Approve the above students</button>
              </form>

              

              </div>
            </div>

  
              


<%- include('../partials/footer') %>